<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Mantenimiento - <%= mantenimiento.placa %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
  >
</head>
<body class="bg-light">

<nav class="navbar navbar-dark bg-dark px-3">
  <span class="navbar-brand">Tomza - Taller Cartago</span>
  <a href="/agenda" class="btn btn-outline-light btn-sm">Volver</a>
</nav>

<div class="container mt-4">

  <h4>Mantenimiento – <strong><%= mantenimiento.placa %></strong></h4>

  <div class="alert alert-secondary mt-3">
    <strong>Tipo:</strong> <%= mantenimiento.tipo %> |
    <strong>Prioridad:</strong> <%= mantenimiento.prioridad || 'MEDIA' %> |
    <strong>Estado:</strong> <%= mantenimiento.estado %>
  </div>

  <!-- ================= PLAN (TALLER) ================= -->
  <div class="card mb-3">
    <div class="card-header fw-bold">Plan (Taller)</div>
    <div class="card-body">

      <% if (user.rol === 'TALLER' || user.rol === 'ADMIN') { %>
        <form method="POST" action="/mantenimientos/<%= mantenimiento.id %>/plan">
          <textarea name="plan" class="form-control mb-2" rows="4" required>
<%= mantenimiento.plan || '' %></textarea>
          <button class="btn btn-primary">Guardar plan</button>
        </form>
      <% } else { %>
        <p class="mb-0"><%= mantenimiento.plan || 'Sin plan definido' %></p>
      <% } %>

    </div>
  </div>

  <!-- ================= EJECUCIÓN (MECÁNICOS) ================= -->
  <div class="card mb-3">
    <div class="card-header fw-bold">Ejecución (Mecánicos)</div>
    <div class="card-body">

      <% if (user.rol === 'MECANICO' || user.rol === 'ADMIN') { %>

        <form method="POST" action="/mantenimientos/<%= mantenimiento.id %>/ejecutar">

          <label class="fw-bold">Trabajos realizados</label>
          <textarea
            name="ejecucion"
            class="form-control mb-3"
router.post("/:id/ejecutar", async (req, res)