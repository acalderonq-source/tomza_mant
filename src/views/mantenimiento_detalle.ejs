<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Mantenimiento - <%= mantenimiento.placa %></title>

  <meta name="viewport" content="width=device-width, initial-scale=1">

  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
  >
</head>

<body class="bg-light">

<!-- NAVBAR -->
<nav class="navbar navbar-dark bg-dark px-3">
  <span class="navbar-brand">
    Tomza - Taller Cartago
  </span>

  <div class="d-flex align-items-center gap-2">
    <span class="text-white small">
      <%= user.usuario %> (<%= user.rol %>)
    </span>
    <a href="/agenda" class="btn btn-secondary btn-sm">
      Volver
    </a>
  </div>
</nav>

<div class="container mt-4">

  <!-- TITULO -->
  <h3 class="mb-3">
    Mantenimiento –
    <strong><%= mantenimiento.placa %></strong>
  </h3>

  <!-- INFO GENERAL -->
  <div class="alert alert-secondary">
    <strong>Tipo:</strong> <%= mantenimiento.tipo %> |
    <strong>Prioridad:</strong> <%= mantenimiento.prioridad || 'MEDIA' %> |
    <strong>Estado:</strong> <%= mantenimiento.estado %>
  </div>

  <!-- ========================= -->
  <!-- PLAN (TALLER / ADMIN) -->
  <!-- ========================= -->
  <div class="card mb-3">
    <div class="card-header bg-dark text-white">
      Plan (Taller)
    </div>

    <div class="card-body">

      <% if (['ADMIN','TALLER'].includes(user.rol)) { %>

        <form
          method="POST"
          action="/mantenimientos/<%= mantenimiento.id %>/plan"
        >
          <div class="mb-3">
            <textarea
              name="plan"
              class="form-control"
              rows="3"
              placeholder="Describa el plan de mantenimiento"
              required
            ><%= mantenimiento.plan || '' %></textarea>
          </div>

          <button class="btn btn-primary btn-sm">
            Guardar plan
          </button>
        </form>

      <% } else { %>

        <p class="mb-0">
          <%= mantenimiento.plan || 'No definido' %>
        </p>

      <% } %>

    </div>
  </div>

  <!-- ========================= -->
  <!-- EJECUCIÓN (MECÁNICOS) -->
  <!-- ========================= -->
  <div class="card mb-4">
    <div class="card-header bg-dark text-white">
      Ejecución (Mecánicos)
    </div>

    <div class="card-body">

      <% if (['ADMIN','MECANICO'].includes(user.rol)) { %>

        <form
          method="POST"
          action="/mantenimientos/<%= mantenimiento.id %>/ejecucion"
        >

          <div class="mb-3">
            <label class="form-label">
              Trabajos realizados
            </label>
            <textarea
              name="ejecucion"
              class="form-control"
              rows="4"
              required
            ><%= mantenimiento.ejecucion || '' %></textarea>
          </div>

          <div class="mb-3">
            <label class="form-label">
              Pendiente
            </label>
            <textarea
              name="pendiente"
              class="form-control"
              rows="3"
            ><%= mantenimiento.pendiente || '' %></textarea>
          </div>

          <button class="btn btn-success">
            Guardar y cerrar mantenimiento
          </button>

        </form>

      <% } else { %>

        <p><strong>Trabajos realizados:</strong></p>
        <p><%= mantenimiento.ejecucion || '—' %></p>

        <p><strong>Pendiente:</strong></p>
        <p><%= mantenimiento.pendiente || '—' %></p>

      <% } %>

    </div>
  </div>

</div>

</body>
</html>
