<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Mantenimiento - <%= mantenimiento.placa %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link
    href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    rel="stylesheet"
  >
</head>
<body class="bg-light">

<nav class="navbar navbar-dark bg-dark px-3">
  <span class="navbar-brand">Tomza - Taller Cartago</span>
  <a href="/agenda" class="btn btn-outline-light btn-sm">Volver</a>
</nav>

<div class="container mt-4">

  <h4>Mantenimiento – <strong><%= mantenimiento.placa %></strong></h4>

  <div class="alert alert-secondary mt-3">
    <strong>Tipo:</strong> <%= mantenimiento.tipo %> |
    <strong>Prioridad:</strong> <%= mantenimiento.prioridad || 'MEDIA' %> |
    <strong>Estado:</strong> <%= mantenimiento.estado %>
  </div>

  <!-- ================= PLAN (TALLER) ================= -->
  <div class="card mb-3">
    <div class="card-header fw-bold">
      Plan (Taller)
    </div>
    <div class="card-body">

      <% if (user.rol === 'TALLER' || user.rol === 'ADMIN') { %>

        <form method="POST" action="/mantenimientos/<%= mantenimiento.id %>/plan">
          <div class="mb-3">
            <textarea
              name="plan"
              class="form-control"
              rows="4"
              placeholder="Qué se le va a realizar a esta unidad"
              required><%= mantenimiento.plan || '' %></textarea>
          </div>

          <button class="btn btn-primary">
            Guardar plan
          </button>
        </form>

      <% } else { %>

        <p class="text-muted mb-0">
          <%= mantenimiento.plan || 'El plan aún no ha sido definido.' %>
        </p>

      <% } %>

    </div>
  </div>

  <!-- ================= EJECUCIÓN (MECÁNICOS) ================= -->
  <div class="card mb-3">
    <div class="card-header fw-bold">
      Ejecución (Mecánicos)
    </div>
    <div class="card-body">

      <% if (user.rol === 'MECANICO' || user.rol === 'ADMIN') { %>

        <form method="POST" action="/mantenimientos/<%= mantenimiento.id %>/ejecutar">
          <div class="mb-3">
            <textarea
              name="ejecucion"
              class="form-control"
              rows="4"
              placeholder="Trabajos realizados"
              required><%= mantenimiento.ejecucion || '' %></textarea>
          </div>

          <button class="btn btn-success">
            Guardar y cerrar mantenimiento
          </button>
        </form>

      <% } else { %>

        <p class="text-muted mb-0">
          <%= mantenimiento.ejecucion || 'Aún no se ha registrado la ejecución.' %>
        </p>

      <% } %>

    </div>
  </div>

</div>

</body>
</html>
