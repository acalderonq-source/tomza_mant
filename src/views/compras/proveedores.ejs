<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <title>Proveedores</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
  <style>
    body {
      background: #f0f2f5;
      font-family: 'Segoe UI', Roboto, sans-serif;
    }
    .card {
      border: none;
      border-radius: 20px;
      box-shadow: 0 8px 20px rgba(0,0,0,0.04);
    }
    @media (max-width: 768px) {
      .table thead { display: none; }
      .table, .table tbody, .table tr, .table td {
        display: block;
        width: 100%;
      }
      .table tr {
        margin-bottom: 1rem;
        border: 1px solid #dee2e6;
        border-radius: 16px;
        padding: 0.8rem;
        background: white;
      }
      .table td {
        text-align: right;
        padding-left: 50%;
        position: relative;
        border-bottom: 1px solid #eee;
      }
      .table td::before {
        content: attr(data-label);
        position: absolute;
        left: 0.8rem;
        width: 45%;
        font-weight: 600;
        text-align: left;
      }
    }
  </style>
  <link rel="stylesheet" href="/css/compras.css">
</head>
<body>

<nav class="navbar navbar-dark bg-dark px-3 py-2">
  <span class="navbar-brand fw-semibold"><i class="bi bi-building"></i> Gestión de Proveedores</span>
  <div class="text-white">
    <%= user.usuario %> (<%= user.rol %>)
    <a href="/compras/ordenes" class="btn btn-sm btn-outline-light ms-2">← Órdenes</a>
    <a href="/dashboard" class="btn btn-sm btn-outline-light ms-2">Dashboard</a>
  </div>
</nav>

<div class="container py-4">
  <div class="page-heading">
    <div>
      <h1 class="page-title">Proveedores</h1>
      <p class="page-subtitle">Directorio de proveedores usados en órdenes y facturas.</p>
    </div>
    <a href="/compras/proveedores/nuevo" class="btn btn-primary">
      <i class="bi bi-plus-circle"></i> Nuevo proveedor
    </a>
  </div>

  <div class="summary-grid">
    <div class="summary-card">
      <div class="summary-label">Proveedores</div>
      <div class="summary-value"><%= proveedores.length %></div>
    </div>
    <div class="summary-card">
      <div class="summary-label">Con email</div>
      <div class="summary-value"><%= proveedores.filter(p => p.email).length %></div>
    </div>
    <div class="summary-card">
      <div class="summary-label">Con teléfono</div>
      <div class="summary-value"><%= proveedores.filter(p => p.telefono).length %></div>
    </div>
    <div class="summary-card">
      <div class="summary-label">Con contacto</div>
      <div class="summary-value"><%= proveedores.filter(p => p.contacto).length %></div>
    </div>
  </div>

  <div class="card p-3">
    <div class="d-flex justify-content-between mb-3">
      <h3 class="page-title fs-5">Listado de proveedores</h3>
    </div>
    <div class="table-responsive">
      <table class="table table-bordered table-hover">
        <thead class="table-light">
          <tr>
            <th>ID</th>
            <th>Nombre</th>
            <th>Teléfono</th>
            <th>Email</th>
            <th>Dirección</th>
            <th>Cédula Jurídica</th>
            <th>Contacto</th>
            <th>Acciones</th>
          </tr>
        </thead>
        <tbody>
          <% proveedores.forEach(p => { %>
            <tr>
              <td data-label="ID"><%= p.id %></td>
              <td data-label="Nombre"><strong><%= p.nombre %></strong></td>
              <td data-label="Teléfono"><%= p.telefono || '—' %></td>
              <td data-label="Email"><%= p.email || '—' %></td>
              <td data-label="Dirección"><%= p.direccion || '—' %></td>
              <td data-label="Cédula Jurídica"><%= p.cedula_juridica || '—' %></td>
              <td data-label="Contacto"><%= p.contacto || '—' %></td>
              <td data-label="Acciones">
                <a href="/compras/proveedores/editar/<%= p.id %>" class="btn btn-sm btn-warning">
                  <i class="bi bi-pencil"></i> Editar
                </a>
                <a href="/compras/proveedores/eliminar/<%= p.id %>" class="btn btn-sm btn-danger" onclick="return confirm('¿Eliminar este proveedor?')">
                  <i class="bi bi-trash"></i> Eliminar
                </a>
              </td>
            </tr>
            <% }) %>
          <% if (proveedores.length === 0) { %>
            <tr><td colspan="8" class="text-center">No hay proveedores registrados</td></tr>
          <% } %>
        </tbody>
      </table>
    </div>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
