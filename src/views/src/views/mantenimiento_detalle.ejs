<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <title>Mantenimiento</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="bg-light">

<div class="container mt-4">

<h3>Mantenimiento – <%= m.placa %></h3>

<div class="mb-3">
  <b>Tipo:</b> <%= m.tipo %> |
  <b>Prioridad:</b> <%= m.prioridad %> |
  <b>Estado:</b> <%= m.estado %>
</div>

<hr>

<h5>Plan (Taller)</h5>
<% if (user.rol === 'TALLER' || user.rol === 'ADMIN') { %>
<form method="POST" action="/mantenimientos/<%= m.id %>/plan">
  <textarea name="plan_texto" class="form-control" rows="4" required></textarea>
  <button class="btn btn-primary mt-2">Guardar plan</button>
</form>
<% } else { %>
  <div class="alert alert-secondary">Solo Taller puede editar</div>
<% } %>

<hr>

<h5>Ejecución (Mecánicos)</h5>
<% if (user.rol === 'MECANICOS' || user.rol === 'ADMIN') { %>
<form method="POST" action="/mantenimientos/<%= m.id %>/ejecucion">
  <label>Realizado</label>
  <textarea name="realizado_texto" class="form-control" rows="3" required></textarea>

  <label class="mt-2">Pendientes</label>
  <textarea name="pendientes_texto" class="form-control" rows="3"></textarea>

  <button class="btn btn-success mt-2">Cerrar mantenimiento</button>
</form>
<% } else { %>
  <div class="alert alert-secondary">Solo Mecánicos puede cerrar</div>
<% } %>

<a href="/agenda/hoy" class="btn btn-secondary mt-3">Volver</a>

</div>
</body>
</html>
